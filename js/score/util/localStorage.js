let storageUtil = {

    /**
     * 没写完，cooike.js的备用方案
     */

    /**
     * 过期时间
     */
    expireTime: 30 * 60 * 60 * 1000,

    /**
     * 保存localstorage数据
     * @param obj.key           键名称
     * @param obj.value         键值 
     * @param obj.expireTime    过期时间
     */
    save: function (obj) {
        if (typeof obj != 'undefined') {
            
        }
    },

    clear: function (obj) {
        
    }
}